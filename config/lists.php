<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Rows per page
    |--------------------------------------------------------------------------
    |
    | Every list in the system pages at the same size. It used to be per
    | controller -- 12 here, 15 there, 30 on the logs -- so the same office
    | machine showed a different amount of scrolling on every screen.
    |
    */

    'per_page' => 10,
];
